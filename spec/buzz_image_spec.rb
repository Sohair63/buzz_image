require 'spec_helper'

describe BuzzImage do
  it 'has a version number' do
    expect(BuzzImage::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
