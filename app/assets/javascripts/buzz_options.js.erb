var __Buzz = {
  options: {
    width: "<%= Rails.application.config.buzz_image.width %>",
    height: "<%= Rails.application.config.buzz_image.height %>",
    zoom_level: "<%= Rails.application.config.buzz_image.buzz_level %>"
  }
}
